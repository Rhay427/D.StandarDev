blank_issues_enabled: false
contact_links:
  - name: Bug in a plugin
    url: https://github.com/Rhay427/D.StandarDev#credits
    about: No plugin source is vendored here — install.sh fetches each one from its own marketplace. Report plugin bugs to that plugin's own repository; the credits table lists who maintains each. Issues here should be about the plugin selection or the install flow.
